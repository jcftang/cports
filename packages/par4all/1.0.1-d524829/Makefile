include ../../../mk/gnu.pre.mk

DISTNAME=       par4all
VERSION=        1.0.1-d524829
CATEGORIES=     devel
HOMEPAGE=       http://www.par4all.org
MASTER_SITES=   http://download.par4all.org/releases/debian/x86_64
MAINTAINER=     jtang@tchpc.tcd.ie
DISTFILES=	$(DISTNAME)-$(VERSION)_src.tar.gz
HAS_CONFIGURE=  no

PKGSUBDIR=	$(DISTNAME)-$(VERSION)_src

DESCRIPTION=    "Par4All is a platform that merges various open source developments to aim at achieving the migration of software to multicore and other parallel processors."

do-build:
	@:

do-install:
	@:

include ../../../mk/gnu.post.mk
