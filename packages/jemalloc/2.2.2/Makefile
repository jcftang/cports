include ../../../mk/gnu.pre.mk

DISTNAME=	jemalloc
VERSION=	2.2.2
HOMEPAGE=	http://www.canonware.com/jemalloc/
MASTER_SITES=	http://www.canonware.com/download/jemalloc
MAINTAINER=	jtang@tchpc.tcd.ie
GNU_CONFIGURE=	yes
DISTFILES=	$(DISTNAME)-$(VERSION).tar.bz2

#DEPENDS+=       "libunwind/0.99 --lib --build-env --run-env"

DESCRIPTION=	"jemalloc is a general-purpose scalable concurrent malloc(3) implementation."

#CONFIGURE_ARGS+=	--enable-prof-libunwind --enable-prof --enable-stats --enable-xmalloc

include ../../../mk/gnu.post.mk
